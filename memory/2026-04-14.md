- Added MiniMax Global as a fallback for Jedai in /home/andrey/.openclaw/openclaw.json.
- Inserted fallback model `minimax/minimax-m1` after Groq Scout for the `main` agent.
- Not verified end-to-end against provider auth/plugin readiness; MiniMax auth/provider may still need setup if not already configured.
